# Runtime dependencies (pins are VERIFY-at-build; freeze exact == in CI once green).
google-genai==2.8.*
aioesphomeapi==45.3.*
httpx>=0.27,<1.0
# soxr>=0.5,<1   # uncomment only if the playback path needs resampling (see PLAN.md §4.5)
