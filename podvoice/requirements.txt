# Runtime lock verified together on Python 3.12.13. Upgrade deliberately and rerun the
# full suite + add-on build; floating versions make yesterday's "green" irreproducible.
aioesphomeapi==45.3.1
httpx==0.28.1
aiohttp==3.14.3   # HA Ingress panel + OpenAI Realtime WebSocket
# NB: soxr/numpy deliberately NOT added — no musllinux wheels for the Alpine base
# image, so they'd break the add-on build. audio.StreamResampler is pure-Python.
