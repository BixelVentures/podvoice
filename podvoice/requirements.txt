# Runtime dependencies (pins are VERIFY-at-build; freeze exact == in CI once green).
aioesphomeapi==45.3.*
httpx>=0.27,<1.0
aiohttp>=3.10   # serves the HA Ingress sidebar panel (web.py) + the OpenAI Realtime WS
# NB: soxr/numpy deliberately NOT added — no musllinux wheels for the Alpine base
# image, so they'd break the add-on build. audio.StreamResampler is pure-Python.
