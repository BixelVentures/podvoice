[project]
name = "podvoice-gatekeeper"
version = "1.13.16"
description = "Danish OpenAI Realtime voice assistant for Home Assistant and PodConnect"
requires-python = ">=3.12"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["podvoice", "tests"]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["podvoice"]
# esphome/ is firmware codegen (depends on the esphome package, not our add-on source).
extend-exclude = ["esphome", "*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "RUF"]
ignore = ["E501"]  # line length enforced by formatter, not lint

[tool.mypy]
python_version = "3.12"
warn_unused_ignores = true
ignore_missing_imports = true
check_untyped_defs = true
files = ["podvoice/gatekeeper"]
